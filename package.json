{
  "type": "module",
  "scripts": {
    "clean": "rimraf src/.observablehq/cache",
    "prebuild": "node src/data/backpacks.json.js && node src/data/cosmetics.json.js && node src/data/users.json.js",
    "build": "observable build",
    "dev": "observable preview",
    "deploy": "observable deploy",
    "observable": "observable"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.3",
    "dotenv": "^16.5.0",
    "googleapis": "^150.0.1",
    "gsap": "^3.13.0",
    "jszip": "^3.10.1",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=18"
  }
}
